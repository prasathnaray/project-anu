info:
  name: Get Volumes By Instructor
  type: http
  seq: 17

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/get-volumes-by-instructor
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
