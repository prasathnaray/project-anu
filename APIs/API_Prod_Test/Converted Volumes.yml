info:
  name: Converted Volumes
  type: http
  seq: 18

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/converted-volumes
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
