info:
  name: Get Volume Placements
  type: http
  seq: 25

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/volume-placements
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
