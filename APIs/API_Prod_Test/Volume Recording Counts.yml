info:
  name: Volume Recording Counts
  type: http
  seq: 22

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/volume-recording-counts
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
