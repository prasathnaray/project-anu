info:
  name: Get Volumes
  type: http
  seq: 15

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/get-volumes
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
