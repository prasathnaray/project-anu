info:
  name: Shadow Recording Counts Legacy
  type: http
  seq: 27

http:
  method: GET
  url: http://{{base_url_prod}}/api/v1/shadow-recording-counts
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
